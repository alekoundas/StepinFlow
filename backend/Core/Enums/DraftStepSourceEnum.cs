namespace Core.Enums
{
    /// <summary>Where a proposed step came from, which is what the wizard shows as its evidence.</summary>
    public enum DraftStepSourceEnum
    {
        RECORDING,
        AI,
    }
}
