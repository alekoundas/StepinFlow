<Page
    x:Class="StepinFlow.Views.Pages.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:StepinFlow.Views.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:StepinFlow.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="DashboardPage"
    d:DataContext="{d:DesignInstance local:DashboardPage,
    IsDesignTimeCreatable=False}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
    ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    Foreground="{DynamicResource TextFillColorPrimaryBrush}"
    mc:Ignorable="d">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <!--<controls:TimeSpanInputUserControl
        x:Name="TimeInputControl"
        Grid.Row="1"
        HorizontalAlignment="Center"
        TotalMilliseconds="{Binding TotalMilliseconds, UpdateSourceTrigger=PropertyChanged}" />-->
        <!--
        Hours="{Binding Hours, UpdateSourceTrigger=PropertyChanged}"
        Milliseconds="{Binding Milliseconds, UpdateSourceTrigger=PropertyChanged}"
        Minutes="{Binding Minutes, UpdateSourceTrigger=PropertyChanged}"
        Seconds="{Binding Seconds, UpdateSourceTrigger=PropertyChanged}"
        -->


        <!--<StackPanel Orientation="Horizontal">
        <TextBox
        x:Name="HoursTextBox"
        Width="40"
        MaxLength="2"
        Text="{Binding Hours, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Margin="2,0" Text=":" />
        <TextBox
        x:Name="MinutesTextBox"
        Width="40"
        MaxLength="2"
        Text="{Binding Minutes, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Margin="2,0" Text=":" />
        <TextBox
        x:Name="SecondsTextBox"
        Width="40"
        MaxLength="2"
        Text="{Binding Seconds, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock Margin="2,0" Text="." />
        <TextBox
        x:Name="MillisecondsTextBox"
        Width="60"
        MaxLength="3"
        Text="{Binding Milliseconds, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>-->
    </Grid>
</Page>
